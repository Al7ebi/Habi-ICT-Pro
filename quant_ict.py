"""\n🔬 وكيل استخراج الأنماط (Quant ICT Agent)\nترتيب وتصفية الأنماط الأقوى\n\"\"\"\n\nfrom dataclasses import dataclass\nfrom typing import List, Dict\nfrom core.ict_logic import (\n    ICTAnalysis, OrderBlock, FairValueGap, Direction\n)\nfrom agents.director import DirectorSignal\nimport logging\n\nlogger = logging.getLogger(__name__)\n\n\n@dataclass\nclass QuantSignal:\n    \"\"\"\n    إشارة من وكيل Quant ICT\n    تجمع الأنماط مع الـ confluence\n    \"\"\"\n    signal_type: str  # \"BUY\" or \"SELL\"\n    strength: float  # 0.0 - 1.0\n    confluence_points: int  # عدد نقاط التقاء\n    entry_level: float\n    stop_loss: float\n    take_profit: float\n    order_block_price: float\n    fvg_price: float\n    liquidity_level: float\n    \n    reasons: List[str]  # الأسباب\n    confluence_details: Dict  # تفاصيل التقاء\n\n\nclass QuantICTAgent:\n    \"\"\"\n    وكيل Quant ICT\n    يستخرج أقوى الأنماط ويرتبها\n    \"\"\"\n    \n    def __init__(self, min_confluence: int = 2):\n        self.min_confluence = min_confluence\n    \n    def analyze(\n        self,\n        ict_analysis: ICTAnalysis,\n        director_signal: DirectorSignal = None\n    ) -> List[QuantSignal]:\n        \"\"\"\n        تحليل الأنماط واستخراج الإشارات\n        \"\"\"\n        signals = []\n        \n        # استخراج إشارات من Order Blocks\n        ob_signals = self._extract_from_order_blocks(\n            ict_analysis,\n            director_signal\n        )\n        signals.extend(ob_signals)\n        \n        # استخراج إشارات من FVGs\n        fvg_signals = self._extract_from_fvgs(\n            ict_analysis,\n            director_signal\n        )\n        signals.extend(fvg_signals)\n        \n        # ترتيب حسب القوة\n        signals = sorted(signals, key=lambda s: s.strength, reverse=True)\n        \n        # فلترة الضعيفة\n        signals = [\n            s for s in signals\n            if s.confluence_points >= self.min_confluence\n        ]\n        \n        return signals[:5]  # أقوى 5 إشارات\n    \n    def _extract_from_order_blocks(\n        self,\n        ict_analysis: ICTAnalysis,\n        director_signal: DirectorSignal = None\n    ) -> List[QuantSignal]:\n        \"\"\"\n        استخراج إشارات من Order Blocks\n        \"\"\"\n        signals = []\n        \n        for ob in ict_analysis.order_blocks[:3]:  # أقوى 3\n            # تحديد نوع الإشارة\n            if ob.direction == Direction.BULLISH:\n                signal_type = \"BUY\"\n                entry = ob.low\n                stop_loss = ob.low - (ob.range * 0.5)\n                take_profit = ob.high + (ob.range * 2)\n            else:\n                signal_type = \"SELL\"\n                entry = ob.high\n                stop_loss = ob.high + (ob.range * 0.5)\n                take_profit = ob.low - (ob.range * 2)\n            \n            # عد نقاط التقاء\n            confluence_points = 1  # OB نفسه\n            confluence_details = {'order_block': ob.strength}\n            reasons = [f\"✅ Order Block {ob.direction.value}\"]\n            \n            # تحقق من FVGs بالقرب من OB\n            nearby_fvgs = self._find_nearby_patterns(\n                ob.low, ob.high, ict_analysis.fair_value_gaps\n            )\n            if nearby_fvgs:\n                confluence_points += len(nearby_fvgs)\n                confluence_details['fvgs'] = len(nearby_fvgs)\n                reasons.append(f\"📊 {len(nearby_fvgs)} FVGs قريبة\")\n            \n            # تحقق من Liquidity بالقرب\n            nearby_liquidity = self._find_nearby_patterns(\n                ob.low, ob.high, ict_analysis.liquidity_levels\n            )\n            if nearby_liquidity:\n                confluence_points += len(nearby_liquidity)\n                confluence_details['liquidity'] = len(nearby_liquidity)\n                reasons.append(f\"💧 {len(nearby_liquidity)} مستويات سيولة\")\n            \n            # تحقق من توافق Director\n            if director_signal:\n                if signal_type == \"BUY\" and director_signal.bias == Direction.BULLISH:\n                    confluence_points += 1\n                    confluence_details['director_bias'] = True\n                    reasons.append(\"📍 توافق مع اتجاه السوق\")\n                elif signal_type == \"SELL\" and director_signal.bias == Direction.BEARISH:\n                    confluence_points += 1\n                    confluence_details['director_bias'] = True\n                    reasons.append(\"📍 توافق مع اتجاه السوق\")\n            \n            # حساب القوة\n            strength = self._calculate_signal_strength(\n                confluence_points,\n                ob.strength,\n                director_signal\n            )\n            \n            signal = QuantSignal(\n                signal_type=signal_type,\n                strength=strength,\n                confluence_points=confluence_points,\n                entry_level=entry,\n                stop_loss=stop_loss,\n                take_profit=take_profit,\n                order_block_price=entry,\n                fvg_price=entry,\n                liquidity_level=entry,\n                reasons=reasons,\n                confluence_details=confluence_details\n            )\n            signals.append(signal)\n        \n        return signals\n    \n    def _extract_from_fvgs(\n        self,\n        ict_analysis: ICTAnalysis,\n        director_signal: DirectorSignal = None\n    ) -> List[QuantSignal]:\n        \"\"\"\n        استخراج إشارات من Fair Value Gaps\n        \"\"\"\n        signals = []\n        \n        for fvg in ict_analysis.fair_value_gaps[:3]:  # أقوى 3\n            if fvg.mitigated:\n                continue  # FVG مغطى\n            \n            # تحديد نوع الإشارة\n            if fvg.direction == Direction.BULLISH:\n                signal_type = \"BUY\"\n                entry = fvg.bottom\n                stop_loss = fvg.bottom - (fvg.range * 0.5)\n                take_profit = fvg.top + (fvg.range * 3)\n            else:\n                signal_type = \"SELL\"\n                entry = fvg.top\n                stop_loss = fvg.top + (fvg.range * 0.5)\n                take_profit = fvg.bottom - (fvg.range * 3)\n            \n            # عد نقاط التقاء\n            confluence_points = 1  # FVG نفسه\n            confluence_details = {'fvg': fvg.gap_size}\n            reasons = [f\"📊 FVG {fvg.direction.value}\"]\n            \n            # تحقق من OBs بالقرب\n            nearby_obs = self._find_nearby_patterns(\n                fvg.bottom, fvg.top, ict_analysis.order_blocks\n            )\n            if nearby_obs:\n                confluence_points += len(nearby_obs)\n                confluence_details['obs'] = len(nearby_obs)\n                reasons.append(f\"✅ {len(nearby_obs)} Order Blocks قريبة\")\n            \n            # تحقق من توافق Director\n            if director_signal:\n                if signal_type == \"BUY\" and director_signal.bias == Direction.BULLISH:\n                    confluence_points += 1\n                    confluence_details['director_bias'] = True\n                    reasons.append(\"📍 توافق مع اتجاه السوق\")\n                elif signal_type == \"SELL\" and director_signal.bias == Direction.BEARISH:\n                    confluence_points += 1\n                    confluence_details['director_bias'] = True\n                    reasons.append(\"📍 توافق مع اتجاه السوق\")\n            \n            # حساب القوة\n            strength = self._calculate_signal_strength(\n                confluence_points,\n                0.7,  # FVG baseline\n                director_signal\n            )\n            \n            signal = QuantSignal(\n                signal_type=signal_type,\n                strength=strength,\n                confluence_points=confluence_points,\n                entry_level=entry,\n                stop_loss=stop_loss,\n                take_profit=take_profit,\n                order_block_price=fvg.bottom if fvg.direction == Direction.BULLISH else fvg.top,\n                fvg_price=entry,\n                liquidity_level=entry,\n                reasons=reasons,\n                confluence_details=confluence_details\n            )\n            signals.append(signal)\n        \n        return signals\n    \n    def _find_nearby_patterns(self, price_low: float, price_high: float, patterns) -> List:\n        \"\"\"\n        البحث عن أنماط قريبة من نطاق معين\n        \"\"\"\n        nearby = []\n        range_size = price_high - price_low\n        buffer = range_size * 0.5  # نصف النطاق كـ buffer\n        \n        for pattern in patterns:\n            if hasattr(pattern, 'high') and hasattr(pattern, 'low'):\n                # Order Block أو Liquidity\n                pattern_center = (pattern.high + pattern.low) / 2\n            elif hasattr(pattern, 'top') and hasattr(pattern, 'bottom'):\n                # FVG\n                pattern_center = (pattern.top + pattern.bottom) / 2\n            elif hasattr(pattern, 'price'):\n                # Liquidity Level\n                pattern_center = pattern.price\n            else:\n                continue\n            \n            if (price_low - buffer) <= pattern_center <= (price_high + buffer):\n                nearby.append(pattern)\n        \n        return nearby[:3]  # أقرب 3 أنماط\n    \n    def _calculate_signal_strength(self, confluence: int, base_strength: float, director: DirectorSignal = None) -> float:\n        \"\"\"\n        حساب قوة الإشارة\n        \"\"\"\n        # القاعدة: confluence\n        strength = min(1.0, 0.3 + (confluence * 0.15))\n        \n        # أضف قوة الـ base\n        strength = (strength + base_strength) / 2\n        \n        # أضف الثقة من Director\n        if director:\n            strength = (strength + director.confidence) / 2\n        \n        return min(1.0, strength)\n    \n    def get_signal_summary(self, signal: QuantSignal) -> str:\n        \"\"\"\n        ملخص الإشارة\n        \"\"\"\n        strength_bars = \"█\" * int(signal.strength * 10)\n        \n        summary = f\"\"\"\n        🔬 إشارة Quant ICT:\n        ══════════════════════════════════\n        النوع: {signal.signal_type} {'🟢' if signal.signal_type == 'BUY' else '🔴'}\n        \n        القوة: {strength_bars} {signal.strength * 100:.0f}%\n        نقاط التقاء: {signal.confluence_points}\n        \n        الأسعار:\n        • Entry:     {signal.entry_level:.2f}\n        • Stop Loss: {signal.stop_loss:.2f}\n        • Take Prof: {signal.take_profit:.2f}\n        \n        الأسباب:\n        {chr(10).join(['• ' + r for r in signal.reasons])}\n        \n        التفاصيل:\n        {chr(10).join([f'  - {k}: {v}' for k, v in signal.confluence_details.items()])}\n        \"\"\"\n        \n        return summary\n"